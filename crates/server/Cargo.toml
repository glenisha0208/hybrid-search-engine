[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.12"
prost = "0.13"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
fusion = { path = "../fusion" }

[build-dependencies]
tonic-build = "0.12"
